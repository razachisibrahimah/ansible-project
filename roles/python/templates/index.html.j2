<!DOCTYPE html>
<html>
<head>
    <title>{{ index_title }}</title> <!-- Title comes from vars/main.yml -->
</head>
<body>
    <h1>{{ index_title }}</h1>        <!-- Header also uses the variable -->
    <p>{{ index_description }}</p>
</body>
</html>
